<?xml version="1.0" encoding="utf-8"?>
<!-- allowBackup is false; this exists so the attribute resolves. -->
<full-backup-content>
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="file" path="." />
</full-backup-content>
