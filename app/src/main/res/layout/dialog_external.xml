<?xml version="1.0" encoding="utf-8"?>
<!--
  "This link leaves Reddit." A custom view rather than AlertDialog's
  setMessage + setItems, because AlertDialog cannot show both: with a message
  set, the item list is silently dropped, and the first version of this dialog
  therefore showed the "no private browser" explanation with no way to
  continue. Every option is a real button here.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <TextView
        android:id="@+id/externalUrl"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="middle"
        android:maxLines="2"
        android:textColor="@color/hint_text"
        android:textSize="13sp" />

    <TextView
        android:id="@+id/externalNote"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="12dp"
        android:textColor="@color/on_top_bar"
        android:textSize="14sp"
        android:visibility="gone" />

    <Button
        android:id="@+id/externalOpen"
        style="?attr/buttonBarButtonStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:gravity="start|center_vertical"
        android:text="@string/external_open" />

    <Button
        android:id="@+id/externalPrivate"
        style="?attr/buttonBarButtonStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="start|center_vertical"
        android:visibility="gone" />

    <Button
        android:id="@+id/externalCopy"
        style="?attr/buttonBarButtonStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="start|center_vertical"
        android:text="@string/external_copy" />
</LinearLayout>
